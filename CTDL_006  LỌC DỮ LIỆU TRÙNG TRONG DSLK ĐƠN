#include <bits/stdc++.h>
using namespace std;

list<int> a;
map <int, int> mp;

int main () {
	int n,k,tmp;
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> tmp;
		mp[tmp]++;
		a.push_back(tmp);
		if (mp[tmp] == 1) {
			cout << tmp << " ";
		}
	}
}
